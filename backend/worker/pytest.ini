[pytest]
# Only collect tests from the dedicated unit-test folder.
# The repository also contains various ad-hoc/manual "test_*.py" scripts in the worker root
# which are not intended for automated CI (some require external deps/services).
testpaths = tests
python_files = test_*.py

